import FeedComponent from "./FeedComponent";

async function Feed(): Promise<JSX.Element> {
  // Add API logic here and pass results as props to FeedComponent
  return <FeedComponent />;
}

export default Feed;