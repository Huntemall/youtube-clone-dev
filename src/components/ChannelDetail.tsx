import ChannelDetailComponent from "./ChannelDetailComponent";

async function ChannelDetail(): Promise<JSX.Element> {
  // Add API logic here and pass results as props to ChannelDetailComponent
  return <ChannelDetailComponent />;
}

export default ChannelDetail;